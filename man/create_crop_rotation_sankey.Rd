% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{create_crop_rotation_sankey}
\alias{create_crop_rotation_sankey}
\title{Create Crop Rotation Sankey Diagram}
\usage{
create_crop_rotation_sankey(
  data,
  min_area = 1,
  exclude_crops = c("grassland", "forest", "flowering area", "fallow", "fruits",
    "permanent/tree", "Meerettich"),
  output_path = NULL,
  width = 14,
  height = 10,
  resolution = 200
)
}
\arguments{
\item{data}{sf object containing crop rotation data with Aggregated_* columns for each year}

\item{min_area}{Minimum area threshold in hectares (default: 1)}

\item{exclude_crops}{Character vector of crop types to exclude}

\item{output_path}{Optional file path for saving the plot}

\item{width}{Plot width in inches (default: 14)}

\item{height}{Plot height in inches (default: 10)}

\item{resolution}{Plot resolution in dpi (default: 200)}
}
\value{
A ggplot object containing the Sankey diagram visualizing crop rotations over time
}
\description{
Creates a Sankey diagram visualizing crop rotation patterns
}
\details{
The function creates a Sankey diagram showing crop rotation patterns over time.
Area values are converted from square meters to hectares in the visualization.
}
\examples{
\dontrun{
library(sf)
# Assuming 'crop_data' is an sf object with Aggregated_* columns
plot <- create_crop_rotation_sankey(
  data = crop_data,
  min_area = 1,
  exclude_crops = c("grassland", "forest")
)
}

}
